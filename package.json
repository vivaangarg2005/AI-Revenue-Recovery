{
  "name": "recover-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Autonomous, policy-bounded AI subscription revenue recovery engine for Razorpay AI Buildathon Track 03",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@recover-ai/shared && npm run dev --workspaces --if-present",
    "build": "npm run build --workspace=@recover-ai/shared && npm run build --workspace=@recover-ai/api && npm run build --workspace=@recover-ai/web",
    "test": "npx vitest run",
    "test:watch": "npx vitest",
    "lint": "npx eslint . --ext .ts,.tsx",
    "format": "npx prettier --write \"**/*.{ts,tsx,json,md}\"",
    "db:format": "npm run db:format --workspace=@recover-ai/api",
    "db:validate": "npm run db:validate --workspace=@recover-ai/api",
    "db:generate": "npm run db:generate --workspace=@recover-ai/api",
    "db:migrate": "npm run db:migrate --workspace=@recover-ai/api",
    "db:seed": "npm run db:seed --workspace=@recover-ai/api",
    "simulation:run": "npx tsx scripts/runSimulation.ts"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
